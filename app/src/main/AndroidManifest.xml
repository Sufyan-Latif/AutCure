<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sufyanlatif.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_my_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SignupActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".activities.TempActivity" />
        <activity android:name="com.soundcloud.android.crop.CropImageActivity" />
        <activity android:name=".activities.UpdateInfoActivity" /> <!-- <activity android:name=".MainActivity"/> -->
        <activity android:name=".activities.AnimationsActivity" />
        <activity android:name=".activities.TeacherSignupActivity" />
        <activity android:name=".activities.ParentSignupActivity" />
        <activity android:name=".activities.AuthenticationActivity" />
        <activity android:name=".activities.TeacherHomeActivity" />
        <activity android:name=".activities.ParentHomeActivity" />
        <activity android:name=".activities.ChildHomeActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.ChildSignupActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".MainActivity" /> -->
        <activity
            android:name=".activities.GameDragActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".activities.GameMatchShapeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.ListNamesActivity" />
        <activity android:name=".PerformanceFinalActivity" />
        <activity
            android:name=".activities.PerformanceActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.TeacherCommunicationActivity" />
        <activity android:name=".activities.AnimalGameActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".activities.VegetableGameActivity" />
    </application>

</manifest>